# پرامپت حرفه‌ای: سیستم آپلود رسانه (Vercel + Parspack) — باگ‌زدایی‌شده و تست‌شده

> این پرامپت یک معماری کامل و **تست‌شده در پروداکشن** را توصیف می‌کند. اگر می‌خواهید همین قابلیت را در پروژه دیگری پیاده کنید، کل این متن را به ایجنت هوش مصنوعی بدهید.

---

## ۱. بستر پروژه

- **فریم‌ورک:** Next.js 15 (App Router) + React + TypeScript — میزبانی روی **Vercel (پلن Hobby)**
- **پایگاه داده:** Postgres روی **Neon** (رایگان) با Prisma 6
- **فضای ابری فایل:** **Parspack Object Storage** (سرویس S3-compatible ایرانی — اتصال با AWS SDK v3، `forcePathStyle: true`، endpoint اختصاصی مثل `https://c463335.parspack.net`)
- **پنل ادمین:** مسیر `/admin`، احراز هویت با JWT (بدون کوکی)؛ همه‌ی APIهای ادمین با `requireAdmin(req)` گارد می‌شوند
- کاربر ایرانی است؛ پیام‌های خطا باید **فارسی** باشند.

## ۲. الزامات (الزامی)

1. **ذخیره نهایی همه‌ی فایل‌ها روی پارسپک** باشد (نه روی ذخیره‌ساز ورسل) — سهمیه ورسل نباید مصرف شود.
2. آپلود عکس ۲ مگابایتی باید روی **شبکه‌ی کند/فیلترشده‌ی کاربر زیر ۳۰ ثانیه** تمام شود (در شبکه‌ی معمولی ~۶ ثانیه).
3. سقف حجم: عکس ۲۵ مگابایت، ویدیو ۱۰۰ مگابایت (mp4/webm). عکس‌ها jpg/png/webp.
4. **محدودیت‌های ورسل Hobby:** بدنه‌ی درخواست حداکثر 4.5MB و تایماوت تابع ۶۰ ثانیه — هیچ درخواستی نباید به این سقف‌ها وابسته باشد.
5. هیچ رکورد دیتابیسی در مسیر ارسال قطعات نباشد (چون با Neon رایگان اتصال‌ها تمام می‌شد).

## ۳. واقعیت‌های میدان (مهم برای طراحی — از تست واقعی)

- **شبکه‌ی کاربر:** درخواست‌های کوچک (~۱۲۸KB) به‌خوبی می‌رسند، اما بدنه‌های بزرگ‌تر (۲۵۶KB به بالا، ۱MB، PUT مستقیم) **قطع یا معلق می‌شوند** و هرگز به سرور نمی‌رسند. پس: «تکه‌تکه‌کردن کوچک» راه‌حل عبور از این شبکه است.
- **پارسپک:** تأخیر متغیر (۲ تا ۹۰+ ثانیه؛ از سرور ورسل ~۱ ثانیه بر هر عملیات). ممکن است درخواست را معلق کند — باید **fail-fast** تنظیم شود و در لایه‌ی کلاینت retry شود.
- **PUT مستقیم مرورگر → پارسپک** (presigned URL) روی شبکه‌ی کاربر کار نمی‌کند؛ همه‌ی ترافیک باید از طریق تابع ورسل برود.
- **نکته‌ی گمراه‌کننده در `vercel logs`:** نماد `ε` یعنی **لاگ میدلور** (edge) است، **نه خطا**! نماد `λ` یعنی اجرای موفق تابع. (در روند دیباگ، این نمادها یک‌بار اشتباهاً «خطا» تعبیر شده بودند و کل مسیر عوض شد — سریع‌تر بررسی کنید.)
- پاسخ‌های خطای سرور باید در پیام کلاینت به‌صورت **final** (بدون retry) سطح‌بندی شوند تا پیام فارسی واقعی به کاربر برسد.

## ۴. معماری نهایی (تست‌شده در پروداکشن)

### ۴.۱ سمت کلاینت (`upload-client.ts`)

1. **فشرده‌سازی محلی:** اگر عکس > ۲MB است، با `canvas` به حداکثر ۲۰۰۰px با کیفیت 0.85 (jpeg) تبدیل شود؛ PNG دست‌نخورده بماند؛ GIF رد شود. (توسط sharp روی سرور هم ابعاد استخراج می‌شود — روی کلاینت فقط canvas.)
2. **پارتیشن‌بندی:** قطعات ۱۲۸KB برای عکس و ۵۱۲KB برای ویدیو (اندازه را سرور در پاسخ `chunk-start` برمی‌گرداند).
3. **ارسال موازی:** ۶ قطعه هم‌زمان؛ هر قطعه با retry تا ۳ بار و backoff صعودی [1.5s, 3s]؛ تایماوت هر درخواست ۳۰ ثانیه با `AbortController`؛ گزارش پیشرفت `(done, total)`.
4. **ترتیب:** `chunk-start` → ارسال قطعات → `chunk-finish`. اگر `chunk-start` با 501 جواب داد → fallback به آپلود multipart تکی (مسیر `upload`).

### ۴.۲ API ها (قراردادها)

- `POST /api/admin/media/chunk-start` — ورودی JSON: `{ fileName, mimeType, size }`
  - اعتبارسنجی نوع (فقط جدول مجاز)، حجم (حداکثر بر اساس `uploadLimits()`)
  - **عکس‌ها:** مرحله‌بندی روی **Vercel Blob** (داخلی ورسل، ~۳۰۰ms، هرگز نمی‌ماند): یک آبجکت متا `tmp/<uploadId>/meta` با `{ expectedSize, chunkSize, mimeType }` و پاسخ `{ ok, uploadId, chunkSize, staging: "blob", baseUrl }`
  - **ویدیوها:** مرحله‌بندی روی پارسپک: متا در `tmp/<uploadId>/meta` و پاسخ `{ ok, uploadId, chunkSize, staging: "s3" }`
  - فرصت‌طلبانه: پاک‌سازی آبجکت‌های رها شده‌ی قدیمی‌تر از ۶ ساعت
- `POST /api/admin/media/chunk` — فرم‌دیتا: `{ uploadId, staging, baseUrl?, index, chunk }`
  - اعتبارسنجی UUID، ایندکس ≤ 9999، اندازه‌ی قطعه ≤ ۵۱۲KB؛ نوشتن قطعه با کلید ثابت `tmp/<uploadId>/000000` (بدون پسوند تصادفی — تا finish بتواند URLها را بازسازی کند). **هیچ دسترسی دیتابیسی.**
- `POST /api/admin/media/chunk-finish` — JSON: `{ uploadId, staging, baseUrl?, alt }`
  - خواندن متا + همه‌ی قطعات → الحاق → **تشخیص نوع با magic bytes** (نه فرمت اعلام‌شده توسط کلاینت) → سقف حجم → ابعاد با sharp → **یک PUT نهایی به پارسپک** (`uploadToStorage`) → ساخت رکورد `media` (با `provider: "s3"`) → حذف قطعات مرحله‌ای. در صورت خطای میانی، هم آبجکت نهایی و هم رکورد پاک شود.

### ۴.۳ پیکربندی‌های حیاتی

- کلاینت S3 باید **fail-fast** باشد:
  - `requestHandler: new NodeHttpHandler({ connectionTimeout: 5000, socketTimeout: 15000 })`
  - `maxAttempts: 2` (نه retry بی‌نهایت SDK — retry در لایه‌ی کلاینت با backoff کوتاه)
  - `requestChecksumCalculation: "WHEN_REQUIRED"` (پارسپک روی aws-chunked گیر می‌کند)
- عکس نهایی روی پارسپک ذخیره شود؛ Vercel Blob فقط **مرحله‌ی میانی موقت** است و بلافاصله بعد از مونتاژ حذف می‌شود (مصرف سهمیه ورسل ≈ صفر).
- احراز هویت فقط JWT (تابع `getSession` هیچ کوئری دیتابیسی ندارد).

## ۵. معیار پذیرش (تست)

1. آپلود عکس ۶.۸MB از شبکه‌ی معمولی: فشرده → ~۱.۵MB → ۱۲ قطعه → 201 در کمتر از ~۱۰ ثانیه؛ رکورد نهایی `provider: "s3"` روی پارسپک.
2. از شبکه‌ی کند/فیلترشده: آپلود عکس ۲MB زیر ۳۰ ثانیه؛ هیچ درخواست بزرگ‌تری از ۱۲۸KB ارسال نشود.
3. فایل‌های نامعتبر (نوع جعلی/حجم زیاد) با پیام فارسی دقیق رد شوند؛ پیام‌های واقعی سرور هرگز با «خطای شبکه» پوشانده نشوند.
4. هیچ رکورد `UploadSession`/`UploadChunk` در دیتابیس باقی نماند (مسیر قطعات کاملاً بدون DB).
5. بعد از هر آپلود، آبجکت‌های موقت `tmp/` (چه روی بلاگ چه روی پارسپک) حذف شده باشند.

## ۶. ساختار فایل‌های پیشنهادی

- `src/lib/upload-client.ts` — منطق کامل کلاینت (فشرده‌سازی، قطعه‌بندی، موازی‌سازی، retry، progress، fallback)
- `src/lib/blob-staging.ts` — مرحله‌بندی روی Vercel Blob (put/get/delete با کلیدهای قطعی، sweep)
- `src/lib/s3-staging.ts` — مرحله‌بندی روی پارسپک (متا/قطعات تحت `tmp/<uploadId>/`)
- `src/lib/storage.ts` — انتزاع ذخیره‌سازی نهایی (`uploadToStorage`, `deleteFromStorage`, `getS3Client` با fail-fast)
- `src/lib/file-validation.ts` — `uploadLimits()` و `detectFileType()` (magic bytes)
- سه route در `src/app/api/admin/media/chunk-start|chunk|chunk-finish/route.ts`
- کامپوننت پیکر رسانه با نمایش «در حال آمادهسازی...» / «در حال آپلود... N٪»

## ۷. نکات تکمیلی

- CSP باید شامل `img-src ... blob:` و `media-src ... blob:` باشد (پیش‌نمایش محلی قبل از آپلود).
- برای دیباگ، در route ها `console.error` بگذارید تا خروجی واقعی در `vercel logs` دیده شود؛ نماد ε=میدلور است، λ=تابع.
- سشن JWT با TTL ۷ روز؛ مشکل «سشن منقضی» در این مسیر وجود ندارد — قبل از فرض آن، لاگ‌ها را ببینید.