# مشخصات طراحی اکوتایمز — استخراج‌شده از اسکرین‌شات‌های مرجع ABC News

> **فاز ۱ — فقط تحلیل.** منبع حقیقت: `reference-design/desktop.png` (1920×10087) و `reference-design/mobile.png` (383×13909).
> هرجا متن پرامپت با اسکرین‌شات تناقض داشت، اسکرین‌شات ملاک است (قانون طلایی ۲).
> نسخه اکوتایمز **آینه‌ی RTL** این چیدمان است (راست↔چپ معکوس می‌شود).

---

## ۱. پالت رنگی (استخراج با پیپت از اسکرین‌شات — مقادیر دقیق)

| توکن | مقدار | محل استفاده در مرجع | نحوه استخراج |
|---|---|---|---|
| `--color-brand` (قرمز اصلی) | **`#c60811`** | بج «Live»، زمان‌های قرمز، زیرخط‌ها، لهجه‌های قرمز | رایج‌ترین قرمز خالص در کل صفحه (۱۲۶۴ پیکسل یکدست) — در هدر، باکس هیرو و سراسر صفحه یکسان |

> ⚠️ نام توکن: به‌دلیل تداخل با `--color-primary` شادسی‌ان (مشکی، مخصوص UI پنل ادمین که نباید عوض شود)، قرمز برند با نام `--color-brand` پیاده شد. مقدار همان `#c60811` قانون ۴ است.
| `--color-navy` (تیره) | **`#000e2c`** | پس‌زمینه Trending Videos، Shop، بنر اپ، فوتر، هدرِ باکس Top Stories | رایج‌ترین رنگ باند فوتر |
| `--color-bg-light` | **`#f4f5f6`** | کارت تایم‌لاین هیرو، بدنه Top Stories، باند Trending Stories، باکس Sponsored | نمونه‌برداری ناحیه‌ای |
| `--color-bg-gma` | **`#dbf2fa`** | باند روشن آبی GMA | نمونه‌برداری ناحیه‌ای |
| `--color-ink` (متن اصلی) | **`#141618`** | تیترهای بولد | نمونه‌برداری از تیتر هیرو |
| `--color-muted` | **`#595b5c`** | متن خاکستری لید/کپشن | نمونه‌برداری |
| `--color-hairline` | **`#cbced4`** | بوردر کارت‌ها و جداکننده‌های نازک | نمونه‌برداری |
| پس‌زمینه پایه | **`#ffffff`** | بدنه اصلی | — |

> ⚠️ **تصمیم:** طبق قانون طلایی ۴ + دستور مستقیم کارفرما (فاز ۵): **هیچ آبی در فرانت‌اند استفاده نمی‌شود.** لیبل‌های دسته‌بندی (که در ABC آبی‌اند) در اکوتایمز **قرمز `#c60811`** هستند. توکن `--color-link` حذف شد.
> ℹ️ قرمز لوگوی اکوتایمز `#c93035` است (از فایل لوگو استخراج شد) — تفاوت با `#c60811` ناچیز و سازگار است؛ ملاک `--color-primary` همان `#c60811` اسکرین‌شات است.

## ۲. تایپوگرافی (نسبی — با وزیرمتن پیاده می‌شود)

| عنصر | وزن | اندازه تقریبی (دسکتاپ) |
|---|---|---|
| تیتر هیرو | 800 | ~30px / line-height 1.25 |
| عنوان بخش‌ها (مثل «Politics ←») | 700 | ~20px |
| تیتر کارت‌ها | 700 | 16–18px / 1.35 |
| لیبل دسته‌بندی زیر عکس | 500 | ~13px |
| زمان/متادیتا | 400 | ~13px |
| تیترهای متنی (لیست‌ها) | 600 | ~15px |
| کپشن عکس / credit | 400 | ~11px خاکستری، روی عکس پایین-راست (در LTR) |

## ۳. نقشه بخش‌های هوم‌پیج دسکتاپ (از بالا به پایین)

عرض محتوا: **~1300px وسط‌چین** (حاشیه‌ها ~16%). گپ گریدها ~16–24px. رادیوس کارت/تصویر ~8px.

| # | بخش | ساختار |
|---|---|---|
| ۱ | **هدر** (سفید، hairline پایین) | ردیف ~64px: همبرگر + لوگو (چپ در LTR → **راست در RTL**)، منو: «• Live» (نقطه قرمز)، Video، Local News▾، Shows▾، Good Morning America، ShopGMA — سمت دیگر: جستجو، زنگوله، دکمه سبز «Stream on Disney+» |
| ۲ | **نوار موضوعات داغ** | «Live Updates» قرمز بولد + لینک‌های متنی با جداکننده `\|`؛ ردیف ~48px، hairline پایین. در موبایل: اسکرول افقی + فلش «‹» |
| ۳ | **هیرو ۳ ستونه** | کارت راست (RTL: اول): bg `#f4f5f6` — تیتر بزرگ + «Live Updates» قرمز + لینک «See All 10+» + تایم‌لاین (زمان قرمز + نقطه قرمز + خط عمودی + تیتر) — مرکز: عکس هیرو ~16:10 با credit — چپ: باکس ویدیوی LIVE نهیوی 16:9 + دکمه پخش + زیرش بج قرمز «• Live» + تیتر بولد. نسبت ستون‌ها ≈ 27/39/33 |
| ۴ | **گرید ۴ کارت + سایدبار Top Stories** | ۴ کارت (عکس 16:9 + لیبل آبی + تیتر ۲خطی) در ~۷۵٪ عرض + سایدبار ~۲۵٪: باکس با هدر نهیوی «Top Stories» و بدنه `#f4f5f6` با ۸ تیتر بولت‌دار (بولت مربع آبی + hairline) — **خودکار از پربازدیدترین‌ها** |
| ۵ | **Trending Videos** (باند نهیوی full-bleed) | عنوان سفید + فلش؛ ستون پلی‌لیست ~۲۵٪ (آیتم: تامب 16:9 + بج مدت‌زمان + لیبل + تیتر سفید) + «Playlist · 12 Videos» با فلش بالا/پایین؛ پلیر بزرگ ~۷۵٪ با دکمه پخش مرکزی |
| ۶ | **بلوک دسته — مدل A (Politics)** | عنوان «Politics ←»؛ گرید ۳ ستونه: ۱ کارت بزرگ + ۲ کارت متوسط؛ زیر هرکدام ۲ تیتر متنی با hairline |
| ۷ | **بلوک دسته — مدل B (کاروسل ویدیو)** | عنوان + فلش‌های prev/next گرد سمت چپ (در RTL)؛ ۳ کارت ویدیو با بج مدت‌زمان روی عکس (پایین-چپ در LTR) |
| ۸ | **بنر اپ** (نهیوی، رادیوس، وسط‌چین) | لوگو + «Download the ABC News App» + زیرمتن + بج‌های استور — **در اکوتایمز: با بنر خبرنامه جایگزین می‌شود** |
| ۹ | **خبر فیچر (اسپلیت)** | تمام‌عرض: ~۵۵٪ عکس + ~۴۵٪ باکس نهیوی (لیبل آبی + تیتر سفید بزرگ + خلاصه خاکستری، وسط‌چین عمودی) |
| ۱۰ | **Trending Stories** (باند `#f4f5f6` full-bleed) | عنوان بولد (بدون فلش)؛ گرید ۳ستونه × ۲ ردیف: تامب مربعی ~80px + لیبل آبی + تیتر + زمان قرمز («1 hour ago») — **خودکار از آخرین اخبار** |
| ۱۱ | **بلوک دسته — مدل C (U.S.)** | ۲ کارت بزرگ ۲ستونه؛ زیرشان گرید ۴ستونه × ۳ ردیف تیتر متنی با hairline |
| ۱۲ | **Shop** (باند نهیوی) | ۳ کارت محصول + فلش کاروسل — **در اکوتایمز: حذف** |
| ۱۳ | **بلوک دسته — مدل D (Business/International)** | ۲ ستون، هرکدام: عکس بزرگ + لیبل + تیتر + گرید ۲ستونه × ۳ ردیف تیتر متنی |
| ۱۴ | **Sponsored** (باکس `#f4f5f6`) | ۵ کارت — **در اکوتایمز: حذف** (سیستم تبلیغات نداریم) |
| ۱۵ | **GMA** (باند `#dbf2fa`) | لوگوی GMA + ۳ کارت + «Latest from GMA» با ۴ کارت کوچک — **در اکوتایمز: حذف** (برند/show معادل ندارد) |
| ۱۶ | **اسلایدشوی عکس تمام‌عرض (Wildfires)** | عنوان وسط‌چین + عکس بزرگ با افکت «دسته کارت» (چند عکس پشت‌سرهم) + کپشن + ناوبری نقطه‌ای + فلش |
| ۱۷ | **ردیف ۴ ستونه پایانی** | Slideshows / Health ← / Climate ← / Technology ← — هر ستون: عنوان + کارت عکس + لیبل + تیتر + ۱–۲ تیتر متنی |
| ۱۸ | **فوتر نهیوی** | لوگوی سفید + ۳ ستون لینک (Topics / Shows / Company) + ردیف آیکون‌های اجتماعی + بج‌های استور + نوار لینک‌های حقوقی + خط کپی‌رایت. در موبایل: ستون‌ها آکاردئونی ▾ |

## ۴. تفاوت‌های موبایل (از اسکرین‌شات موبایل)

- هدر تک‌ردیفه: همبرگر + لوگو + جستجو + زنگوله؛ نوار موضوعات اسکرول افقی
- هیرو: **همه stack** — عکس هیرو تمام‌عرض → کارت تیتر/تایم‌لاین → باکس LIVE → ۴ کارت تمام‌عرض
- Top Stories: تمام‌عرض بعد از کارت‌ها
- Trending Videos: پلیر تمام‌عرض اول، پلی‌لیست زیرش
- همه گریدها ۱ ستونه؛ فوتر آکاردئونی
- بج مدت‌زمان ویدیو: پایین-چپ تامب (هم‌راستا با قانون ۹ برای آیکون پخش اکوتایمز)

## ۵. جدول نگاشت: بخش‌های فعلی (بیمه‌نیوز) ←→ ABC News

| بخش فعلی | معادل ABC | تصمیم |
|---|---|---|
| `CommodityTicker` (نمادهای بیمه‌ای) | — | **حذف** — معادلی در ABC نیست و محتوایش بیمه‌ای است؛ نوار موضوعات داغ (breaking) جایگزین نوار افقی می‌شود |
| `TopNewsStrip` (breaking + hero) | هیرو ۳ ستونه + نوار موضوعات | **نگاشت** — breaking → نوار «Live Updates» + تایم‌لاین کارت؛ hero → عکس مرکزی |
| — | باکس ویدیوی LIVE کنار هیرو | **جدید** — پخش زنده نداریم؛ پیشنهاد: «آخرین ویدیو» (آخرین پست دارای ویدیو) — موضوع تأیید کارفرما در فاز ۵ |
| — | سایدبار Top Stories | **نگاشت** از `getMostViewedPosts` (موجود ✅) |
| `LatestVideosSection` | Trending Videos | **نگاشت** — بخش `videos` موجود ✅ |
| `SocialEnergySection` / `EconomyNewsSection` (بلوک دسته) | مدل A/C/D بلوک‌های دسته | **نگاشت** — الگوی مشترک بلوک دسته برای هر ۹ دسته |
| `MagazineSection` (ماهنامه) | — | **حذف** — اکوتایمز ماهنامه ندارد |
| `MultimediaSection` (گالری) | اسلایدشوی Wildfires + ستون Slideshows | **نگاشت** |
| `LatestArticlesSection` | Trending Stories | **نگاشت** — `getLatestPosts` موجود ✅ |
| — | بنر اپ | **جایگزین با بنر خبرنامه** (نمونه موجود در فوتر فعلی) |
| — | Shop / Sponsored / GMA | **حذف** |
| `SiteFooter` | فوتر نهیوی ۳ ستونه | **بازسازی** — ستون Topics = ۹ دسته جدید |

## ۶. ترتیب پیشنهادی بخش‌های هوم‌پیج اکوتایمز (RTL)

1. هدر (لوگو راست) → ۲. نوار «به‌روزرسانی زنده» (breaking) → ۳. هیرو ۳ستونه → ۴. گرید ۴ کارت + سایدبار پربازدیدترین‌ها → ۵. **ویدیوهای پرطرفدار** (نهیوی) → ۶. **هوش مصنوعی** (مدل A) → ۷. **اقتصاد دیجیتال** (مدل B کاروسل) → ۸. بنر خبرنامه → ۹. **سلامت و درمان** (فیچر اسپلیت) → ۱۰. **داغ‌ترین‌ها** (باند خاکستری، خودکار) → ۱۱. **زیست‌فناوری و نانو** (مدل C) → ۱۲. **میکروالکترونیک + انرژی** (مدل D دوتایی) → ۱۳. اسلایدشوی عکس (گالری) → ۱۴. ردیف ۴ستونه: **آب و محیط‌زیست / امنیت غذایی / صنایع خلاق / اسلایدشو** → ۱۵. فوتر

> هر ۹ دسته دقیقاً یک بار پوشش داده می‌شوند ✅

## ۷. نیازمندی‌های جدید بک‌اند (ورودی فاز ۳)

| # | نیاز | وضعیت |
|---|---|---|
| ۱ | فیلد `homepageVideoUrl` + `homepageVideoType` (UPLOAD/APARAT/YOUTUBE) روی Post — برای قابلیت پخش ویدیو روی تصویر (قانون ۹) | **جدید — اضافه شود** |
| ۲ | کلیدهای HomeSection جدید: `breaking`, `hero`, `videos`, `photos`, `feature` + ۹ کلید دسته (`ai`, `digital-economy`, `health`, `biotech-nano`, `microelectronics`, `energy`, `water-environment`, `food-security`, `creative-industries`) | **seed جدید** (مکانیزم موجود) |
| ۳ | ۹ دسته جدید با slug انگلیسی | **seed جدید** |
| ۴ | سایدبار پربازدیدترین / آخرین اخبار خودکار | موجود ✅ |
| ۵ | بج مدت‌زمان ویدیو (`videoDuration`) و پارسر آپارات/یوتیوب | موجود ✅ |
| ۶ | «See All 10+» = شمارش پست‌های breaking | محاسباتی — بدون تغییر schema |
